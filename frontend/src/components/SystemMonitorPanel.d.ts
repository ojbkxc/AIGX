/**
 * SystemMonitorPanel 类型声明桥接。
 * 运行时实现仍在 SystemMonitorPanel.jsx（协作者维护），此文件仅提供静态类型，
 * 使 TypeScript 页面（Security.tsx 等）可以无声明错误地导入。
 */
declare const SystemMonitorPanel: () => JSX.Element;

export default SystemMonitorPanel;
